# 待决策事项

| ID | 来源位置 | 问题 | 影响范围 | 当前临时处理方式/推荐默认值 | 状态 |
|---|---|---|---|---|---|
| DEC-01 | PRD M1 输入表单 | 页面首次打开时，计算方式切换控件默认选中「固定利率法」还是「递减余额法」？冻结定型稿未指定初始选中项。 | M1（初始渲染状态）、M4（切换逻辑的起点） | 推荐默认选中「固定利率法（Fixed Interest）」，理由：定型稿行文顺序中固定利率法排在前。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-02 | PRD M2 输入校验 | 定型稿只说「不允许负数」，未明确贷款金额为 0、总期限（年+月）为 0 是否算无效输入。若允许 0，M3 计算引擎会出现除以 0 或空贷款的无意义结果。 | M2（校验规则）、M3（计算边界处理） | 推荐：贷款金额必须 > 0；期限（年+月）之和必须 > 0。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-03 | PRD M2 输入校验 | 定型稿页面流程未提及「计算」提交按钮，只描述「填写字段 → 系统校验 → 计算」。是否为实时校验+实时计算（无按钮），还是需要显式提交按钮？ | M2（校验触发时机）、M3/M4（计算触发时机） | 推荐：字段值变化即实时校验、实时计算，不设提交按钮。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-04 | PRD M2 输入校验 | 当已展示结果的字段被改为非法值时，结果摘要（M5）与摊还明细表（M6）应隐藏/清空，还是保留上一次的有效结果直到重新校验通过？ | M2、M5、M6 | 推荐：校验失败时隐藏/清空结果区域，避免显示与当前输入不一致的旧结果。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-05 | PRD M2 输入校验 | 定型稿未提及是否需要处理非数字字符输入（如字母）。假设输入框本身限制为数字输入类型，非数字字符无法输入，因此校验范围只覆盖「负数/零」这类数值范围问题。 | M2（校验规则范围） | 推荐：依赖浏览器原生数字输入限制，PRD 不额外定义非数字字符的校验规则。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-06 | PRD M3 计算引擎 | 定型稿只说「金额精度 2 位小数，向上取整」，未说明取整是应用在内部逐期计算中，还是只应用在最终展示值上。逐期取整会放大累积误差。 | M3（计算精度）、M6（明细表展示） | 推荐：内部计算使用完整精度（不做中间取整），仅对最终展示值（月供/总利息/总还款额/每期本金·利息·剩余本金）做 2 位小数向上取整。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-07 | PRD M3 计算引擎 | 展示层取整会导致逐期本金部分累加之和与贷款金额本身出现尾差（最后一期剩余本金可能不精确为 0）。如何收口？ | M3（摊还表最后一期计算）、M6（展示） | 推荐：最后一期本金部分 = 贷款金额 − 前 n-1 期本金部分展示值之和，强制最后一期剩余本金精确为 0.00。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-08 | PRD M3 计算引擎 | 递减余额法标准公式 `P × r × (1+r)^n / ((1+r)^n − 1)` 在年利率为 0 时分母为 0，需要特殊处理，定型稿未提及此边界。 | M3（递减余额法公式实现） | 推荐：年利率为 0 时，monthlyPayment = 贷款金额 / 总期数，每期利息部分 = 0。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-09 | PRD M5 结果摘要展示 | 定型稿要求「非并排对比、手动切换查看」，但未说明结果摘要区域是否要标注当前对应的是哪种计算方式。若不标注，用户切换后可能分不清当前结果属于哪种算法。 | M5（结果摘要展示） | 推荐：在摘要区域标注当前选中的计算方式名称（如 `Fixed Interest` / `Reducing Balance`）。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-10 | PRD M6 摊还明细表 | 长期限贷款（如 30 年 = 360 期）会产生数百行摊还明细。定型稿未提及是否需要分页/虚拟滚动等性能相关处理。 | M6（摊还明细表渲染方式） | 推荐：MVP 阶段直接一次性渲染全部行，不做分页/虚拟滚动（纯前端小规模数据，性能可接受；暂缓范围亦未提及此类能力）。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-11 | PRD 非功能要求 | 定型稿未提及浏览器兼容性范围。 | 非功能要求（兼容性） | 推荐：支持主流浏览器最新两个大版本（Chrome、Firefox、Safari、Edge），不保证旧版浏览器（如 IE）兼容性。 | Deferred by user（2026-07-16，接受推荐默认值） |
| DEC-12 | PRD 非功能要求 | 定型稿未提及是否需要移动端/平板响应式布局。产品定位为「面向所有人」的公开 Web 工具，可能存在移动端访问需求。 | 非功能要求（响应式布局）、M1/M6（表单与表格在小屏幕下的展示） | 推荐：支持移动端/平板响应式布局，摊还明细表在小屏幕下允许横向滚动。 | Deferred by user（2026-07-16，接受推荐默认值） |
