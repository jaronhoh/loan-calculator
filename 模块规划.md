# 模块规划

> 依据：冻结版产品定型稿 `loan-calculator/shaping/06-最终定型稿.md`（v1.0，冻结日期 2026-07-16）。
> 说明：当前环境未找到用户指定或 skill 自带的 PRD 模板文件（`assets/templates/`），本规划与后续 PRD 正文使用默认标准结构。若你有公司 PRD 模板，请提供文件供后续对齐。

## 模块拆分依据

按功能行为、状态流转、数据依赖差异拆分：输入与展示分离，校验单独成模块（阻塞后续计算），两种计算方式的核心公式差异归入同一计算引擎模块（共享输入/输出数据结构与舍入规则），切换交互单独成模块（涉及状态保留与重新计算的特定规则），结果摘要与摊还明细表因展示形式和数据粒度不同拆分为两个模块。

## 模块列表

| 模块 ID | 模块名称 | 范围说明 | 页面/入口 | 依赖关系 | 建议开发顺序 | 可并行 | 评审状态 |
|---|---|---|---|---|---|---|---|
| M1 | 输入表单 | 贷款金额、年利率（默认 4%）、期限（年/可选月）字段渲染与默认值 | 单页应用主界面 | 无 | 1 | — | Draft |
| M2 | 输入校验 | 金额/利率/期限负数校验，行内错误提示，校验失败阻止计算 | 主界面（表单区） | 依赖 M1 | 2 | — | Draft |
| M3 | 计算引擎 | 固定利率法与递减余额法公式实现；金额精度（2 位小数）与向上取整规则；输出月供/总利息/总还款额/逐期摊还数据 | 无独立 UI，被 M5/M6 调用 | 依赖 M1、M2（仅校验通过后计算） | 3 | — | Draft |
| M4 | 计算方式切换 | 固定利率法/递减余额法手动切换（非并排对比），已输入数值保留，切换后按新方式重新计算 | 主界面（切换控件） | 依赖 M1、M3 | 4 | 可与 M5/M6 并行开发（接口先约定） | Draft |
| M5 | 结果摘要展示 | 展示月供、总利息、总还款额 | 主界面（结果区） | 依赖 M3 | 5 | 可与 M6 并行 | Draft |
| M6 | 摊还明细表 | 逐期期数、月供、本金部分、利息部分、剩余本金列表 | 主界面（明细表区） | 依赖 M3 | 5 | 可与 M5 并行 | Draft |

## 全局约束（非独立模块，适用于所有模块）

- 纯前端 SPA，无后端、无数据库，无持久化；关闭/刷新浏览器后状态清空，恢复默认初始态。
- 英文界面，仅 MYR。
- 无账号体系、无权限分级。

## 建议开发顺序说明

M1 → M2 → M3 为串行主链路（表单 → 校验 → 计算）。M4（切换交互）与 M5/M6（结果展示）都依赖 M3 的输出数据结构，接口约定后可并行开发。

## 评审状态

整体状态：**Confirmed**（用户 2026-07-16 确认模块拆分与开发顺序）。PRD 正文已于 2026-07-16 冻结（v1.0）。

各模块正文撰写状态：

| 模块 ID | 正文状态 |
|---|---|
| M1 | 已确认 |
| M2 | 已确认 |
| M3 | 已确认 |
| M4 | 已确认 |
| M5 | 已确认 |
| M6 | 撰写中，待确认 |
